# Supplied runtime provenance

Copied from the user's local archive:
`LUMINA_XC_LIVE_BRIDGE_v0.3.2-coupled-environment.zip`

- `runtime/xc.py` -> `vendor/xc.py` (XC runtime v0.3.1.2)
- `program/lumina_core.xc` -> `vendor/lumina_core.xc` (LuminaCore v0.5)

Both files are unchanged. The game compiles in-memory copies of the core with
different seed declarations for the four birds. No existing LUMINA saves are read
or modified. No license file was present in the supplied archive listing; this
document records provenance and does not grant redistribution rights.
